apiVersion: v2
name: worker
description: Async consumers — process RabbitMQ jobs (orders, email, indexing).
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: 0.1.0
    repository: "file://../common"
