apiVersion: v2
name: api
description: API service (Laravel / Node.js) — REST/GraphQL gateway for clients.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: 0.1.0
    repository: "file://../common"
